<Window x:Class="Vaccindate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vaccindate"
        Height="550"
        Width="700"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="30">
        <StackPanel>

            <TextBlock Text="Podaj datę urodzenia:"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="0 0 0 10"/>

            <DatePicker x:Name="BirthDatePicker"
                        FontSize="16"
                        Margin="0 0 0 20"/>

            <Button Content="Pokaż szczepienia"
        Height="40"
        FontSize="15"
        Click="CheckVaccinationButton_Click"
        Margin="0 0 0 20"/>

            <TextBox x:Name="ResultsTextBox"
         FontSize="14"
         TextWrapping="Wrap"
         AcceptsReturn="True"
         VerticalScrollBarVisibility="Auto"
         HorizontalScrollBarVisibility="Disabled"
         IsReadOnly="True"
         Height="350"/>

        </StackPanel>
    </Grid>
</Window>